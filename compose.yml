networks:
  traefik:
    external: true
  trupesound-internal:
    name: trupesound-internal

volumes:
  postgres_data:
  pgadmin_data:

services:
  db:
    image: postgres:16-alpine
    container_name: trupe-sound-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: trupesound
      POSTGRES_PASSWORD: trupesound
      POSTGRES_DB: trupesound
    networks:
      - trupesound-internal
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trupesound"]
      interval: 5s
      timeout: 5s
      retries: 10
    logging:
      driver: json-file
      options:
        tag: "db"
        max-size: "10m"
        max-file: "3"

  backend:
    image: ghcr.io/mphennrichs/trupesound-backend:latest
    container_name: trupe-sound-backend
    restart: unless-stopped
    environment:
      # Use the unique container name, NOT "db": the shared traefik network has
      # other containers (tag-anything-db, ibex_docs-db-1) aliased "db", which
      # caused Prisma P1000 by resolving to the wrong Postgres.
      DATABASE_URL: postgresql://trupesound:trupesound@trupe-sound-db:5432/trupesound
      NODE_ENV: production
      APP_URL: https://trupesound.foguinhodogoias.com
      # Storage — set ONE mode. Cloud (SeaweedFS):
      STORAGE_ENDPOINT: ${STORAGE_ENDPOINT}
      STORAGE_ACCESS_KEY: ${STORAGE_ACCESS_KEY}
      STORAGE_SECRET_KEY: ${STORAGE_SECRET_KEY}
      # Local mode (comment out cloud vars above and set this instead):
      # STORAGE_LOCAL_FOLDER: /sounds
      JWT_SECRET: ${JWT_SECRET}
    networks:
      - traefik
      - trupesound-internal
    depends_on:
      db:
        condition: service_healthy
    logging:
      driver: json-file
      options:
        tag: "backend"
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/ping || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.trupesound-api.rule=Host(`trupesound.foguinhodogoias.com`) && PathPrefix(`/api`)"
      - "traefik.http.routers.trupesound-api.entrypoints=web-secure"
      - "traefik.http.routers.trupesound-api.tls.certresolver=cloudflare"
      - "traefik.http.routers.trupesound-api.priority=10"
      - "traefik.http.services.trupesound-api.loadbalancer.server.port=3000"

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: trupe-sound-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: mphennrichs@gmail.com
      PGADMIN_DEFAULT_PASSWORD: trupesound
    networks:
      - traefik
      - trupesound-internal
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      db:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.trupesound-pgadmin.rule=Host(`trupedb.foguinhodogoias.com`)"
      - "traefik.http.routers.trupesound-pgadmin.entrypoints=web-secure"
      - "traefik.http.routers.trupesound-pgadmin.tls.certresolver=cloudflare"
      - "traefik.http.services.trupesound-pgadmin.loadbalancer.server.port=80"

  frontend:
    image: ghcr.io/mphennrichs/trupesound-frontend:latest
    container_name: trupe-sound-frontend
    restart: unless-stopped
    networks:
      - traefik
      - trupesound-internal
    depends_on:
      - backend
    logging:
      driver: json-file
      options:
        tag: "frontend"
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      # Main router — basic auth required
      - "traefik.http.routers.trupesound.rule=Host(`trupesound.foguinhodogoias.com`)"
      - "traefik.http.routers.trupesound.entrypoints=web-secure"
      - "traefik.http.routers.trupesound.priority=1"
      - "traefik.http.routers.trupesound.middlewares=trupesound-auth"
      - "traefik.http.services.trupesound.loadbalancer.server.port=80"
      - "traefik.http.routers.trupesound.tls.certresolver=cloudflare"
      - "traefik.http.middlewares.trupesound-auth.basicauth.removeHeader=true"
      - "traefik.http.middlewares.trupesound-auth.basicauth.users=lucoelho:$$2y$$05$$yVDK8eByB5LUO7MnWYHfZuNMfms/QV/aYS4e0Dl.rBTE4yw8DlEe6"
      # how to set a real password:
      # sudo apt-get install apache2-utils
      # htpasswd -Bnb username password | sed -e s/\\$/\\$\\$/g
      # Public router — no auth for PWA manifest (browsers fetch it without credentials)
      - "traefik.http.routers.trupesound-public.rule=Host(`trupesound.foguinhodogoias.com`) && Path(`/manifest.json`)"
      - "traefik.http.routers.trupesound-public.entrypoints=web-secure"
      - "traefik.http.routers.trupesound-public.priority=2"
      - "traefik.http.routers.trupesound-public.tls.certresolver=cloudflare"
